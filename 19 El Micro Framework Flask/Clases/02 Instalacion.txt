1 crear un nuevo directorio 
2 usar "python3 -m venv venv" si eres mac 
    para windows es "py -3 -m venv y venv",
    aca nos creara en el direcctorio la careta venv 
3 ahora aplicamos el comando en la carpeta creada con anterioriad no la venv el siguiente comando
    ". venv/bin/activate" este es el comando de mac 
    ".venv/Scrips/activate" para windows
4 e instalamos con el comando "pip3 install Flask"
