lista = [1,2,3]
lista_2 = [1,2,3,4]
# Queremos ver solo un valor de la lista, para esto vemos cuantos elementos tiene con el len
print(len(lista))
# Vemos que en la 'Lista' tiene 3 elementos y ahora para sacar un elemento de esta 
#vamos a ver que valor tiene la posicion x donde x es el dato que quiero ver 
#como en todo lenguaje inicia desde el 0 "0 = 1, 1 = 2 y 2 = 3"
#ahora vemos el elemento 0 de la lista 
print(lista[0])
