En la sección pasada nosotros vimos She s greet y CSS Grid.

Es el sistema que nosotros utilizamos como guía para poder distribuir el contenido a lo largo de nuestra

aplicación.

Por lo general, cuando nosotros estamos trabajando con CSS Grid vamos a ver algo muy similar a esto,

donde vamos a tener a cada uno de estos elementos divididos de manera equitativa a lo largo de esta

grilla.

Entonces es que yo voy a definir un ancho para una columna.

Voy a tener el mismo ancho para toda la columna 1, también para toda la columna 2 y también para toda

la columna 3, donde las columnas para ser esta parte que se encuentra acá.

En el caso de que yo quiera definir también un alto para las filas.

Voy a poder definir también estos mismos actos.

Voy a aprovechar de cambiar esto a una gomita.

Achicar esto un poco.

Y voy a poder definir también el alto de éstas, pero en su eje horizontal, o sea, hacia el lado.

Está, por supuesto que van a quedar distribuidas de esta manera.

Esto es sumamente importante, ya que cuando nosotros estemos pensando en distribuir el contenido de

nuestra aplicación, no solo vamos a poder pensar en ésta como si fuese una planilla de Excel.

Nosotros vamos a tener un alto como estamos mostrando aquí y también vamos a poder tener un ancho como

tenemos aquí.

También lo puedo hacer aquí abajo y esto nos va a permitir a nosotros distribuir los contenidos, nuestro

contenido, pero de una forma que no solo vamos a tener disponible en el caso de Flex Box.

Esto funciona un poco distinto.

No es mucho más, pero.

Estábamos aprovechando en el logro.

En el caso de Flex Box, ésta está pensada para que nosotros tengamos filas, una única fila o una única

columna.

Entonces está pensado para distribuir contenidos, pero en una sola dimensión.

A diferencia de CSS Grid, que está pensado en Google, el contenido de dos dimensiones.

Cuando nosotros empezamos a distribuir el contenido con Flex Box, vamos a poder hacerlo con columnas

y también con filas.

Vamos a hacer el ejemplo solamente de las filas.

Nosotros vamos a tener esto acá, que va a ser el contenedor de todo nuestro contenido y vamos a ir

colocando nuestros elementos dentro de esta fila de Flex Box.

Sin embargo, cuando nosotros lleguemos aquí al final y no tengamos más espacio para poder seguir agregando

contenido, en este caso, lo que va a hacer la grilla de Flex Box es pasar a la parte de abajo.

Y aquí, en esta parte de abajo, si es que nosotros tenemos un elemento que puede ser incluso más delgado

que estos, los va a utilizar así, y el siguiente elemento va a utilizar el espacio siguiente.

Como pueden ver, en este caso, las filas o las columnas no quedan completamente alineadas.

En este caso, las filas sí, porque nosotros tenemos una distribución de tipo fila, pero las columnas

van a tener una separación entre ellas.

Y no necesariamente tienen que encajar en la misma, en el mismo lugar donde debiese estar una columna,

en el caso que no sólo la definen con CSS Grid.

La principal diferencia de CSS Grid con Flex Box es que Flex Box está pensado en manejar una única fila

y una única columna, o sea, elementos de una sola dimensión.

Mientras CSS Grid está pensado en manejar elementos de múltiples dimensiones.
