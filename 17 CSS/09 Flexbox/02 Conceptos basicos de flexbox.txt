La distribución de nuestro contenido con Flex Box se va a basar en lo siguiente Vamos a partir utilizando

este container que se encuentra acá, que nosotros podemos ver que es como una fila.

Luego de eso, nosotros vamos a ir depositando todos los ítems dentro de este contenedor.

Pero estos van a utilizar el ancho que ellos tengan que utilizar.

Este vendría siendo Tapate de acá nuestro inicio principal.

Vamos a colocarle un main start y esto que se encuentra acá vendría siendo nuestro main end.

Nuestro eje principal por el cual nosotros vamos a distribuir todo nuestro contenido va a ser este que

se encuentra acá.

Le vamos a dar el nombre de eje principal

y también vamos a tener un eje cruzado, que es este que se encuentra acá.

Esto, por supuesto, es que nos va a dar un comienzo, un cross start y también un cross end, que

van a ser en el sentido de nuestro eje cruzado, por lo que esta parte de acá, de hecho, todo este

fin de acá que es a lo largo de todo esto, va a ser nuestro cross start.

Y este de acá abajo va a ser nuestro cross end.

Esto por supuesto que se encuentra acá.

El ancho vendría siendo nuestro tamaño principal.

Y también vamos a tener este tamaño de acá, que vendría siendo el alto finalmente de nuestro contenedor

de flex, que esto es el tamaño Kross.

Podemos dejarlo también en inglés.

El Kross 6.

Y por supuesto que cada uno de estos elementos que se encuentra acá adentro corresponde, corresponde

a un flex item.

Y por supuesto que el contenedor, esta cajita en particular corresponde a un flex container.

Esta parte sumamente importante, ya que nosotros lo vamos a hacer, vamos a hacer esa distinción cuando

nosotros estemos hablando de los items o también cuando estemos hablando de todo este contenedor.

